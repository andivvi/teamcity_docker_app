# За основу используется базовый образ Node.js, использую актуальную версию на alpine.

FROM node:15.12.0-alpine3.10

#Определяю рабочую директрию проекта /api/

WORKDIR /usr/src/app

# Переношу скрипты проета в директорию образа.

COPY server.js ./
COPY package.json ./

# Устанавливаю необходимый софт

RUN npm install

#Объявляю на каком порту будет работать приложение в данном приложении это порт 8080

EXPOSE 8080

# Выполняю команду запуска проекта.

CMD ["npm", "start"]

